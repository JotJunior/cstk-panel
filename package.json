{
  "name": "cstk-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Dashboard de Observabilidade Read-Only para execucoes agente-00c/feature-00c",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:readonly-check": "grep -rniE '\\b(INSERT|UPDATE|DELETE|CREATE|DROP|ALTER)\\b' apps/server/src && echo 'FAIL: mutation verbs found' && exit 1 || echo 'OK: no mutation verbs'",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
